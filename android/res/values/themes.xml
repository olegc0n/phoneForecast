<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework-only theme: no AppCompat, so the build needs no AndroidX jars. -->
    <style name="Shelf" parent="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
